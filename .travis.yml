language: python
python:
  - "2.7"
# command to install dependencies
install:
  - pip install .
  - pip install coveralls
  - pip install flake8

# command to run tests
script:
  - flake8 git_aggregator --exclude=__init__.py
  - coverage run --source git_aggregator setup.py test

after_success:
  - coveralls
